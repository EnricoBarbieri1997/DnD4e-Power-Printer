# .github/workflows/release.yml

on:
  push:
    branches:
      - release

jobs:
  release:
    name: release ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-gnu
          - target: x86_64-unknown-linux-musl
          - target: x86_64-apple-darwin
    steps:
      - uses: actions/checkout@master
      - name: Extract version
        id: extract_version
        run: echo "VERSION=$(cat Cargo.toml | grep version | head -n1 | cut -d' ' -f3 | sed 's/\"//g')" >> "$GITHUB_OUTPUT"
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }} ${{ steps.extract_version.outputs.VERSION }}
          body: |
            Release ${{ steps.extract_version.outputs.VERSION }}
            ${{ github.event.head_commit.message }}
          draft: false
          prerelease: false
      - name: Compile
        id: compile
        uses: rust-build/rust-build.action@v1.4.4
        with:
          RUSTTARGET: ${{ matrix.target }}
          UPLOAD_MODE: none
      - name: Package
        id: package
        run: |
          unzip ${{ steps.compile.outputs.BUILT_ARCHIVE }} -d dist/
          mkdir -p dist/user/characters/
          mkdir -p dist/user/printables/
          cp -r data/ dist/
          zip -9r archive.zip dist/
          path="$(pwd)/archive.zip"
          echo "BUILT_ARCHIVE=$path" >> "$GITHUB_OUTPUT"
      - name: Upload
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_name: ${{ matrix.target }}
          asset_path: ${{ steps.package.outputs.BUILT_ARCHIVE }}
          asset_content_type: application/zip
